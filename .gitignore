.DS_Store
.idea/
.claude/settings.local.json

dist/
public/
cache/
node_modules/

# Scratch area for unoptimized source photos before new-post.sh imports them.
local/

# Local Netlify folder
.netlify

# Codex writes session state under the user home (~/.codex), not the repo.
.codex/

# Global Claude-CLI tooling (caveman / cavecrew) lives in ~/.claude, not the repo.
.agents/skills/caveman*
.agents/skills/cavecrew
